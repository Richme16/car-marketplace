// Change this ONE line when your backend goes live — every other file reads from here.

// Live backend (Render):
const API_ROOT = "https://bra-kay-motorline.onrender.com";

// Local development (uncomment this and comment out the line above if working locally):
// const API_ROOT = "http://localhost:5000";
